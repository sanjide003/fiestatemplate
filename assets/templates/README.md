# Fixed poster and certificate backgrounds

Place the approved PNG files here without renaming them: `rslt.png`, `fst.png`, `scd.png`, `trd.png`, `cert.png`, `lead-id.png`, `jdg-id.png`, `team-id.png`, `gst-id.png`, and `vol-id.png`.

The Admin **Poster & Certificate** editor stores only field geometry and styles in Firestore. Background images remain version-controlled repository assets.
